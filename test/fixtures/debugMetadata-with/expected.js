(function() {
  if (false) {
    normalSetup();
  } else {
    debugSetup();
  }

  var Elm = {};
  Elm['Main'] = Elm['Main'] || {};
  if (typeof _user$project$Main$main !== 'undefined') {
      _user$project$Main$main(Elm['Main'], 'Main', {});
  }
}).call(this);