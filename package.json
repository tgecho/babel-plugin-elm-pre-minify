{
  "name": "babel-plugin-elm-pre-minify",
  "version": "0.1.0",
  "description": "A Babel plugin that prepares Elm compiled Javascript for optimal minification",
  "main": "elm-pre-minify.js",
  "repository": "https://github.com/tgecho/babel-plugin-elm-pre-minify",
  "author": "Erik Simmler <tgecho@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "nyc --reporter=html --reporter=text --reporter=text-summary --check-coverage --lines 100 --functions 100 --branches 100 ava"
  },
  "devDependencies": {
    "ava": "^0.18.2",
    "nyc": "^10.1.2",
    "prettier": "^0.22.0"
  }
}
